{
  "name": "groupbuy",
  "private": true,
  "description": "根目录脚本：构建 web 并部署 Firebase Hosting",
  "scripts": {
    "build": "npm run build --prefix web",
    "deploy": "npm run build --prefix web && firebase deploy --only hosting",
    "firebase": "firebase"
  },
  "devDependencies": {
    "firebase-tools": "^14.24.0"
  }
}
